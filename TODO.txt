/*
 *sqlite - Master file + update
 * Auch eine allgemeine Settings datei?
 * Gut für eine Liste mit allen Channels und deren Verwaltung.
 *
 */

Tabelle: sqlite_master
Felder:  type, name, sql
         type='table'

sql="CREATE TABLE settings (bezeichnung TEXT PRIMARY KEY, wert TEXT)"
sql .replace("CREATE TABLE "+name)
sql.trim();
// IRGENDWIE DIE () am anfang und ende löschen
sql.split(",")

sql[] <= Die Felder die es gibt, mit dem jeweiligen typen!

//// if not exists?!?
ALTER TABLE "name" ADD COLUMN "sql[]"
